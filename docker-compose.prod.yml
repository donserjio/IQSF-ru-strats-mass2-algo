services:
  app:
    image: ${APP_IMAGE}
    container_name: iq-fund-algo
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 5000
    ports:
      - "127.0.0.1:5012:5000"

